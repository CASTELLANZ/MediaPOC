name: Deploy to GitHub Pages

on:
push:
branches:
\- main \# 또는 master 등 기본 브랜치 이름

# 워크플로우에 저장소 쓰기 권한 부여

permissions:
contents: write

jobs:
build-and-deploy:
runs-on: ubuntu-latest
steps:
\- name: Checkout 🛎️
uses: actions/checkout@v4
